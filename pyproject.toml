[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "memos"
version = "1.5.0b1"
description = "Universal, local-first, persistent memory layer for AI agents, LLM apps, and chatbots."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Markgatcha" },
]
keywords = [
    "memory",
    "ai",
    "llm",
    "agent",
    "graph",
    "sqlite",
    "local-first",
    "ollama",
    "langchain",
    "chatbot",
    "persistent-memory",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
<<<<<<< HEAD
    "fastapi>=0.139.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.13.4",
=======
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.50.2",
    "pydantic>=2.0.0",
>>>>>>> 0ec9e5d (chore(deps): update uvicorn requirement from >=0.32.0 to >=0.50.2)
]

[project.optional-dependencies]
langchain = [
    "langchain>=0.3.0",
    "langchain-core>=1.4.8",
]
ollama = [
    "ollama>=0.6.2",
]
all = [
    "memos[langchain,ollama]",
]
dev = [
    "ruff>=0.15.20",
    "pytest>=8.0.0",
    "pytest-asyncio>=1.4.0",
    "httpx>=0.27.0",
    "mypy>=2.1.0",
]

[project.urls]
Homepage = "https://github.com/Markgatcha/memos"
Repository = "https://github.com/Markgatcha/memos"
Issues = "https://github.com/Markgatcha/memos/issues"
Documentation = "https://memos.readthedocs.io"

[project.scripts]
memos-server = "server.main:main"

[tool.hatch.build.targets.wheel]
packages = ["server", "adapters"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "B008",   # do not perform function calls in argument defaults (FastAPI)
]

[tool.ruff.lint.isort]
known-first-party = ["server", "adapters"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
