"""Smoke tests — verify the test suite can be collected and run."""


def test_always_passes() -> None:
    """Placeholder test that always passes.

    Real tests will be added as the Python SDK is developed.
    """
    assert True
