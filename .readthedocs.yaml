# Read the Docs configuration for memos.readthedocs.io
# Docs: https://docs.readthedocs.io/en/stable/config-file/v2.html
#
# After importing this repo at https://readthedocs.org/dashboard/import/,
# the site builds automatically on every push to main.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false

python:
  install:
    - requirements: docs/requirements.txt
