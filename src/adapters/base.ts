/**
 * Storage adapter base interface.
 *
 * Re-exports the {@link StorageAdapter} contract from `types.ts` so that
 * backend implementations can import from `@memos/adapters/base`.
 *
 * @module @memos/adapters/base
 */
export type { StorageAdapter } from "../types";
